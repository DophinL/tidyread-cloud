RSS 中的文章，会在生成简报时进行拉取。

拉取的数量取决于插件设置页面中的 `Max Items Per RSS Feed` 的值。
拉取的时间范围取决于 阅读源编辑页面的 `Time Span` 的值。

比如，`Max Items Per RSS Feed` 的值为 10，某个阅读源的`Time Span` 的值为 1 天，那么这个阅读源最多保留 10 篇文章（可能不到 10 篇，因为过去一天可能并没有发布 10 篇文章）。
